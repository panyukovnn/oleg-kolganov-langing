---
layout: null
# The plugin's default template uses collection document dates and static-file
# modification times. A checkout or rebuild changes those without changing the
# published content, so only an explicitly maintained last_modified_at is used.
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% assign collections = site.collections | where_exp: 'collection', 'collection.output != false' %}
  {% for collection in collections %}
    {% assign docs = collection.docs | where_exp: 'doc', 'doc.sitemap != false' %}
    {% for doc in docs %}
      <url>
        <loc>{{ doc.url | replace: '/index.html', '/' | absolute_url | xml_escape }}</loc>
        {% if doc.last_modified_at %}<lastmod>{{ doc.last_modified_at | date_to_xmlschema }}</lastmod>{% endif %}
      </url>
    {% endfor %}
  {% endfor %}
  {% assign pages = site.html_pages | where_exp: 'doc', 'doc.sitemap != false' | where_exp: 'doc', 'doc.url != "/404.html"' %}
  {% for page in pages %}
    <url>
      <loc>{{ page.url | replace: '/index.html', '/' | absolute_url | xml_escape }}</loc>
      {% if page.last_modified_at %}<lastmod>{{ page.last_modified_at | date_to_xmlschema }}</lastmod>{% endif %}
    </url>
  {% endfor %}
  {% assign static_files = site.static_files | where_exp: 'file', 'file.sitemap != false' | where_exp: 'file', 'file.name != "404.html"' %}
  {% for file in static_files %}
    {% if file.extname == '.htm' or file.extname == '.html' or file.extname == '.xhtml' or file.extname == '.pdf' %}
      <url>
        <loc>{{ file.path | replace: '/index.html', '/' | absolute_url | xml_escape }}</loc>
        {% if file.last_modified_at %}<lastmod>{{ file.last_modified_at | date_to_xmlschema }}</lastmod>{% endif %}
      </url>
    {% endif %}
  {% endfor %}
</urlset>
