# Справочник тарифов

## Область задачи

База: f56d5147bc004f5f651cc4625eb1d001219eb134. Ветка: fix/tariffs-core-reference-20260915.
Обновить /fnc/tarify/index.html из канонического notary-tariff-bitrix, сохранить все действия и старые якоря категорий. Новые CSS/JS ограничены справочником. Общие шаблоны и калькулятор не изменяются.

## Обновление

`node scripts/sync-tariff-reference.mjs --core PATH` импортирует loadCatalog() из канонического проекта. `--check` проверяет отсутствие расхождений без записи. В репозитории сайта хранится только публичная проекция каталога, без runtime, клиентов, настроек сервера и секретов.

Файл editorial содержит исходное значение и согласованную редакцию каждого очищенного поля. Если исходное поле меняется, экспорт останавливается: редакцию нужно пересмотреть. Числовые формулы федерального тарифа импортируются без изменений. Контрольные суммы исходных файлов находятся в метаданных данных.

Проверки: экспорт --check, node --test tests/tariff-reference.test.mjs, Jekyll build, настольный и мобильный предпросмотр, поиск и прямые ссылки, git diff --check; повторный fetch и сравнение с origin/main перед передачей.

## Расхождения исходника

В примечании action_042 пропущено «не»: «дополнительный региональный тариф за них взимается». Для сайта исправлено по п. 42 официального документа МГНП (редакция 22.01.2026, действует с 26.01.2026): https://mgnp.info/wp-content/uploads/files/upth/rrt_20260126.pdf. Числовая формула не менялась. Исправление текста нужно отдельно перенести в канонический проект при его следующем выпуске. Это не исправление уже работающего калькулятора.

Прочие редакции устраняют обрывки переноса таблицы, дубли федеральных шкал в региональной колонке и повторы. action_065 сохраняет базовую ставку и явно показывает освобождение 100% рядом с ней.

## Параллельная работа

Владение файлами этой задачи: docs/fnc/tarify/index.html, docs/_data/tariff_reference.json, docs/static/css/custom/tariff-reference.css, docs/static/js/custom/tariff-reference.js, scripts/sync-tariff-reference.mjs, maintenance/tariff-reference*, tests/tariff-reference.test.mjs.
Перед слиянием снова fetch, проверить изменения этих путей и выполнить сборку объединённой версии. Отсутствие текстового конфликта не доказывает совместимость изменений общих шаблонов. Не применять force push или замену всего каталога docs. Продакшен выпускается после просмотра и явного одобрения владельца.
