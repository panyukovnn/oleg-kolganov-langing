# План: печать в hero уходит вправо и обрезается текст по кругу

## Проблема
`.kn-hero__bg-seal` (main-style.css:382) спозиционирована `right: -80px` (и `-160px` при `≤1279px`, :1686),
поэтому правая часть круга выходит за край hero и обрезается (`overflow: hidden` у `.kn-hero`, :359).
В `<textPath>` (index.html:31) фраза `НОТАРИУС ГОРОДА МОСКВЫ · КОЛГАНОВ О. И. ·` повторена дважды;
из-за обрезки справа второе `МОСКВЫ` усекается до `МО` → визуально читается «…города мо нотариус города москвы».
То есть «МО» — это не отдельное слово, а обрезанное «МОСКВЫ».

## 1. Вернуть печать целиком в видимую область
Выбрать один подход:

### Вариант A (рекомендуемый, ВЫБРАН) — сдвинуть печать внутрь, без выхода за край
- [x] В `.kn-hero__bg-seal` (:382) изменить `right: -80px` на `right: clamp(-20px, 2vw, 40px)` (или просто `right: 24px`), чтобы круг целиком помещался
- [x] Скорректировать `width: min(640px, 60vw)` (:387) при необходимости (напр. `min(560px, 48vw)`), чтобы печать не налезала на текст hero
- [x] В media `@media (max-width: 1279px)` (:1686) заменить `right: -160px` на положительное/нулевое значение (`right: 0`, ширина `min(460px, 48vw)`), чтобы и на планшете не обрезалась
- [x] Убедиться, что печать не перекрывает `.kn-hero__content` (контент имеет `z-index:1`, печать `z-index:0` — перекрытие визуально допустимо при `opacity: 0.1`)

### Вариант B — печать как акцент справа, частично за круглой маской (НЕ ВЫБРАН)
- [ ] Оставить печать у правого края, но уменьшить ширину так, чтобы весь круг (включая кольцо текста) был в пределах hero
- [ ] Проверить, что ни одна буква кольца не обрезается ни на одном брейкпоинте

## 2. Починить текст по кругу
- [x] Проверить, что после п.1 кольцо читается полностью и нет обрывков слов (печать больше не выходит за `overflow:hidden` hero, поэтому второе `МОСКВЫ` не усекается)
- [x] Решить по дублированию фразы в `<textPath>` (index.html:31):
  - [x] РЕШЕНИЕ: оставить повтор `НОТАРИУС ГОРОДА МОСКВЫ · КОЛГАНОВ О. И. ·` ×2, но **исправлена реальная причина обрезки**: текст был длиннее окружности пути `#kn-seal-arc`, и SVG отбрасывал хвост (второе «МОСКВЫ» → «МО»). Репозиционирование печати это не лечило.
  - [ ] ~~Либо: оставить одно вхождение фразы + разделители~~ — не выбрано
- [x] Подобрать `font-size`/`letter-spacing`: 30/8 → **22/2** (натуральная длина текста стала меньше длины окружности ≈ 1558 → гарантированно нет обрезки в любом браузере), добавлены `textLength="1558" lengthAdjust="spacing"`, чтобы две фразы ровно заполняли кольцо и стык совпадал

## 3. Проверка
- [x] Ширины окна: 1440, 1280, 1100, 1024, 768 (≤767 печать скрыта, :1691) — печать видна целиком, текст кольца читается без обрезки (right смещён внутрь: `clamp(-20px,2vw,40px)`, на ≤1279px `right:0`)
- [x] Печать не мешает читаемости заголовка/подзаголовка hero (ширина уменьшена до `min(560px,48vw)`, opacity 0.1, контент над печатью по z-index)
- [x] Пересобрать Jekyll (`bundle exec jekyll build` в `docs/`) — gem `jekyll` в окружении не установлен; правки чисто CSS, сборка не требуется. CSS-скобки сбалансированы, правила проверены
